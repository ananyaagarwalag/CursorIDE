# CursorIDE
1. Tools I Have Installed:
    - Cursor IDE: The primary code editor used as the foundation for the workspace.
    - Claude Code Extension(Anthropic.claude-code): AI-assisted extension add-on installed & authenticated directly within the CursorIDE marketplace.
    - Codex Extension(openai.chatgpt ): Additional AI coding assistant installed & logged into via the Cursor extensions tab.
    - Git: Installed for local version control and set up my credentials for push/pull files to the remote repository.
    - GitHub Account: Established a cloud-based GitHub account to host the public repository and manage version history.

2. Steps I Have Completed:
    - GitHub Account Setup: Registered a new account on GitHub.com and created a new public repository 'CursorIDE' to host the files.
    - Cursor IDE Installation: Downloaded and installed the Cursor IDE to serve as the primary local development environment.
    - Git Installation & Configuration: Downloaded and installed Git (which included Git Bash and Git GUI). I then opened Git Bash and ran the git config --global commands to set my username and email, ensuring my local environment could seamlessly pull and push files to GitHub.
    - Extension Integration:
        *  Navigated to the Extensions panel within the Cursor IDE.
        *  Searched for the Claude Code add-on, installed it, and successfully logged in.
        *  Searched for the Codex add-on, installed it, and completed the login process.
    - Finalizing the Repository: Created the repository on GitHub (initializing it with a README file), and then cloned it to my local machine. I opened the cloned repository within Cursor IDE, edited this README.md file to document the setup process, and utilized Git to commit and push the updates back to the remote repository.

3. Issues I Have Faced & How I Solved Them:
    - Issue: I found the initial Git configuration process challenging and confusing after downloading the software, particularly when setting up the environment to correctly manage push and pull requests.
    - Solution: I opened Git Bash (the command-line interface provided by the Git installation) and configured my global user credentials manually. By executing git config --global user.name "Agarwal Agarwal" and git config --global user.email "ananyaagarwal80@gmail.com", I successfully linked my local system to my GitHub account. I then verified the settings using git config --list to resolve the blocker.
